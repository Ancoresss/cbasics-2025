#include <stdio.h>
int main() {
	int a, b;
	int W, Q;

        scanf("%d", &a);
	scanf("%d", &b);

	W = a * a;
	Q = b * b;
        printf("W = %d\n", W);
       	printf("Q = %d\n", Q);

	printf("W = %d, Q = %d\n", W, Q);

	return 0;
}
